import { NextApiRequest, NextApiResponse } from "next";

import { TAnggota } from "@/types";

export default function handler(
  req: NextApiRequest,
  res: NextApiResponse<TAnggota>
) {
  // res.status(200).json({  })
};
